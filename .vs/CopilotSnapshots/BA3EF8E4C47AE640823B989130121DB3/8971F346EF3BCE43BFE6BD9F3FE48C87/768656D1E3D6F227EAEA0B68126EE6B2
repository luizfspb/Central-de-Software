using System;
using System.Windows.Forms;

public class UserControlFiscal : UserControl
{
    private Panel panelMenuFiscal;
    private Button btnXmlEditor;
    private Panel panelContentFiscal;

    public UserControlFiscal()
    {
        InitializeComponent();
    }

    private void InitializeComponent()
    {
        this.panelMenuFiscal = new Panel { Width = 200, Dock = DockStyle.Left, BackColor = System.Drawing.Color.FromArgb(240, 240, 240) };
        this.btnXmlEditor = new Button { Text = "Editor de XML da NFe", Top = 20, Left = 20, Width = 160, Height = 40 };
        this.btnXmlEditor.Click += (s, e) => ShowFiscalFunction(new UserControlFiscalXmlEditor());
        panelMenuFiscal.Controls.Add(btnXmlEditor);

        this.panelContentFiscal = new Panel { Dock = DockStyle.Fill };
        this.Controls.Add(panelContentFiscal);
        this.Controls.Add(panelMenuFiscal);

        ShowFiscalHome();
    }

    private void ShowFiscalFunction(UserControl control)
    {
        panelContentFiscal.Controls.Clear();
        control.Dock = DockStyle.Fill;
        panelContentFiscal.Controls.Add(control);
    }

    private void ShowFiscalHome()
    {
        var label = new Label
        {
            Text = "Escolha uma função fiscal ao lado.",
            AutoSize = false,
            Width = 400,
            Height = 40,
            Top = 40,
            Left = 40,
            Font = new System.Drawing.Font("Segoe UI", 16),
            TextAlign = System.Drawing.ContentAlignment.MiddleLeft
        };
        panelContentFiscal.Controls.Clear();
        panelContentFiscal.Controls.Add(label);
    }
}
